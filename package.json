{
  "name": "lineage-worker",
  "version": "1.0.0",
  "description": "A random family tree generator, refactored as a Cloudflare Worker.",
  "private": true,
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "npx vitest",
    "build": "npx esbuild src/client.js --bundle --outfile=public/bundle.js",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "dotenv -e .env -- npx wrangler deploy",
    "predeploy": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "esbuild": "^0.25.10",
    "vitest": "^3.2.4",
    "wrangler": "^3.0.0"
  }
}
